version: '3.7'
services:
  vnc-app:
    image: vnc:latest
    container_name: vnc-app
    network_mode: bridge
    # restart: always
    environment:
      - DISPLAY_WIDTH=1920
      - DISPLAY_HEIGHT=899
      - VNC_PASS=123456
    ports:
      - 6080:6080/tcp
